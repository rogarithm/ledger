x 지출 금액 합 계산
x 기간 내 지출 금액 합 계산
 x 특정일에 지출한 총 금액 계산
  x 하드코딩된 일자에 지출한 총 금액 계산
  x 변수로 입력한 날에 지출한 총 금액 계산
 x 특정 기간에 지출한 총 금액 계산
	x 하드코딩한 시작일부터 종료일까지 지출 내역 추출
	x 명령 실행할 때마다 날짜 목록 초기화
	x 변수로 입력한 시작일부터 종료일까지 날짜 목록 추출
	x 기간 내 지출 내역 추출
	x 기간 내 지출한 총 금액 계산
 - x일 전 쓴 총 금액 계산
 - 지난 주에 해당하는 날짜를 mm.dd 형식으로 추출
 - 명령을 실행하는 요일에 상관없이 지난 주 7일에 쓴 총 금액을 계산
- 특정 기간 동안 특정 계좌에서 쓴 총 금액 계산
- 잔액 계산
 - 현재 잔액 계산
- 한 달에 배정한 예산 중 쓴 돈 계산
- 테이블 열 정렬

- 카테고리별 지출내역 가져오기 구현 방법
 1 category 파일에 카테고리 적고 이 파일 읽어서
 2 지출 내역에서 $(카테고리 필드 | sort -u) 결과를 써서
