x 가계부에 적은 리스트의 총액을 계산한다
- 특정 기간 동안의 쓴 총 금액을 계산한다
 x 특정일에 지출한 총 금액을 계산한다
  x 하드코딩된 일자에 지출한 총 금액을 계산한다
  x 특정일을 변수로 나타내 그날 지출한 총 금액을 계산한다
 - 특정 기간에 지출한 총 금액을 계산한다
	- 기간의 시작일과 종료일을 인자로 받아 해당하는 날짜 모두를 알아낸다
	x 하드코딩한 시작일과 종료일 사이 기간에 포함되는 날짜의 지출 내역을 가져온다
	- 기간에 포함되는 날짜의 지출 내역을 가져온다
	- 기간에 포함되는 날짜의 총 지출액을 계산한다
 - x일 전 쓴 총 금액을 계산한다
 - 지난 주에 해당하는 날짜를 mm.dd 형식으로 추출
 - 명령을 실행하는 요일에 상관없이 지난 주 7일에 쓴 총 금액을 계산한다
- 특정 기간 동안 특정 계좌에서 쓴 총 금액을 계산한다
- 현재 남은 돈을 계산한다
- 한 달에 배정한 예산 중 얼마를 썼는지를 계산한다
- 테이블 열을 맞춘다

- 카테고리별 지출내역 가져오기 구현 방법
 1 category 파일에 카테고리 적고 이 파일 읽어서
 2 지출 내역에서 $(카테고리 필드 | sort -u) 결과를 써서
