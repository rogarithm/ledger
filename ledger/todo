- 지출내역 편하게 입력하기
x 지출 금액 합 계산
x 기간 내 지출 금액 합 계산
- 잔액 계산
 - 한 달에 배정한 예산 중 쓴 돈 계산
 - 현재 잔액 계산
  - 검증을 어떻게 하지?
- 특정일에 지출한 총 금액 계산
 - 매개변수가 하나면 두번째 매개변수를 첫번째 매개변수 값과 동일하게 한다
- 특정 계좌에서 쓴 총 금액 계산
- 명령을 실행하는 요일에 상관없이 지난 주 7일에 쓴 총 금액을 계산
 - x일 전 쓴 총 금액을 계산
 - 지난 주에 해당하는 날짜를 mm.dd 형식으로 추출
- 테이블 열 정렬
- 카테고리별 지출내역 가져오기 구현 방법
 1 category 파일에 카테고리 적고 이 파일 읽어서
 2 지출 내역에서 $(카테고리 필드 | sort -u) 결과를 써서
- 테스트 정리
 x 테스트를 테스트 디렉토리로 옮긴다
 x 테스트 디렉토리에서 테스트를 실행 가능하도록 바꾼다
- kakao로 검색했을 때 kakaopay도 같이 검색되는 문제 정규표현식으로 해결하기
